@page
@model MVAIntermediate.Pages.Orders.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.OrderMasters[0].TableId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.OrderMasters[0].Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.OrderMasters[0].OrderDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.OrderMasters[0].WaiterName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderMasters) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TableId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WaiterName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OrderNo">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OrderNo">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OrderNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
